<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="reservation">
    <select id="findAll" resultType="reservation">
        select
            *
        from
            reservations
    </select>
    <insert id="insertReservation">
        insert into
            reservation
        values
            (
                ('reservation' || lpad(seq_reservation_no.nextval,3,0)), #{restNo}, #{usersNo}, #{reservName} ,sysdate, sysdate, #{reservPeople}, #{request}, default, default
            )
    </insert>
</mapper>